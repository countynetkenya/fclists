"""Configuration for FCLists docs."""

source_link = "https://github.com/countynetkenya/fclists"
headline = "Dense computed lists + QuickBooks-POS-style transaction histories for ERPNext"
sub_heading = "Role-gated Script Reports over native ERPNext doctypes — no fork, no client logic."


def get_context(context):
	context.brand_html = "FCLists"
